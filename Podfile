# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'Miksing' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks! --> dynamic frameworks crash
  use_modular_headers!
  
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'Firebase/Storage'
  
  target 'MiksingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MiksingUITests' do
    # Pods for testing
  end

end
